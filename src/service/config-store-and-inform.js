
export const storePublishedConfigAndInformBroker = async (logger) => {
  logger.info('storing config in s3')
  // store config in s3

  logger.info('informing config broker new version to release')
  // inform config broker new version to release
}
